"""
ui/admin_panel_backup.py

Módulo de backup do painel administrativo do Integragal.

ATENÇÃO:
    - Este módulo NÃO é utilizado no fluxo principal da aplicação.
    - A implementação original foi removida por estar desatualizada e conter múltiplos erros de sintaxe.
    - Ele é mantido apenas como referência histórica / placeholder.
"""


class AdminPanelBackup:
    """
    Placeholder para o painel administrativo legado.

    Qualquer tentativa de instanciar esta classe levantará um erro explícito,
    para deixar claro que ela não deve ser utilizada no sistema atual.
    """

    def __init__(self, *args, **kwargs) -> None:
        raise RuntimeError(
            "AdminPanelBackup é um placeholder legado e não deve ser "
            "utilizado no sistema atual."
        )
